# Code-Health Audit — Dynamic Research (S183, night of 2026-06-28→29)

**Method:** MRPF AUDIT gate, run as a frozen-safe **parallel multi-agent Workflow** — 6 read-only dimension agents (file-size/conciseness, architecture/boundaries, skills, security, tests/deps/debt, regenerative+parallel-agent adherence) → 1 synthesis/completeness-critic pass. 7 agents, ~948k tokens, 199 tool-uses, ~8 min wall-clock. No code touched (repo frozen for the Aira demo).
**Trigger:** pre-demo health sweep ("make sure nothing binds us tomorrow") + ongoing codebase hygiene.
**Git at audit time:** main `2b9a7d2` (clean + the S183 report).

## VERDICT: 🟢 Strong & demo-ready — ZERO demo-blockers

The demo path (OTP login → dashboard → run detail → 14 deliverables) exercises only **session-org-scoped read routes against Supabase Storage**. It touches no worker code, no new-run serialization, and no cross-tier type mirror — so the entire class of drift/concurrency/size debt below is **structurally unable to surface during the demo.** Every finding is post-freeze hardening.

### Positive baseline (independently verified by the audit)
- **Executor decomposition landed cleanly** — `executor.ts` 849 LOC (from 2247) over a 33-module **acyclic** DAG, zero modules reaching back into executor internals.
- **Tenant isolation correct by construction** — every demo-path route derives `orgId` from the session (no request-supplied org anywhere); storage boundary = session-derived `<orgId>/<slug>/<file>` prefix → cross-org read structurally impossible. (S183 also proved this at runtime: foreign-slug → 404.)
- **Prompt-injection fenced** at both agent + frontend tiers; markdown deliverables DOMPurify-sanitized.
- **RLS enabled on all 5 perimeter tables** (CHECK-NOT-NULL closes the service-role silent-NULL gap).
- **No secrets committed/logged.**
- **~803 tests green, 0 skipped/.only**; dependency posture conservative (3 advisories, all dev-toolchain-only — js-yaml/@babel/esbuild via eslint/tsx — none reach the Vercel bundle or a worker port).
- **No runtime self-modifying code** (`eval`/`new Function` → 0 hits) → the regenerative sandbox guardrail has no live surface to violate.
- **Run-firing skills (`/research-compare`, `/promote`) are NOT auto-triggered** → the skill layer cannot fire a rogue run during demo-prep chat.

## Ranked findings — all POST-FREEZE (do not fix tonight)

| # | Sev | Finding | Location | Effort |
|---|---|---|---|---|
| 1 | medium | **untrusted-input prompt-injection fence pair is SECURITY-load-bearing, guarded only by a comment pair-edit rule (no parity test) — and has already diverged structurally** (agent grew `fenceValue()`/`jsonAndEscape()` the frontend lacks; escape bodies byte-identical *today*) | `agent/lib/untrusted-input.ts` vs `frontend/lib/untrusted-input.ts` | small |
| 2 | medium | **Job-update field allowlist maintained in 4 places, silently no-ops on drift** (route.ts:111-114 literally warns fields "get silently dropped") | `agent/api-client.ts:76`; `frontend/lib/validate.ts:391`; `frontend/app/api/queue/[id]/route.ts:84`; DB | small |
| 3 | medium | **ResearchJob/PipelineState type mirror (~280 LOC) — largest undefended cross-tier contract, already partially diverged**; `PipelineState` (state.json contract) exists only agent-side | `agent/types.ts:204-320` vs `frontend/lib/types/queue.ts:190-244` | medium |
| 4 | medium | **Freshly-extracted/orchestration modules lack companion tests** — led by `plan-review-gate.ts` (the proceed/abort gate before the costly spawn), plus worker.ts poll-tick, mcp-proxy, PHASE_CHECKS | `agent/lib/plan-review-gate.ts`; `agent/worker.ts`; `agent/mcp-proxy/index.ts` | medium |
| 5 | low | **Worker daemon uses NO concurrency primitives — every async loop is strictly serial** (clearest gap vs the "parallelism wherever possible" goal). The `/research-compare` skill IS parallelized; the gap is the worker's I/O tails (14 deliverable uploads serial; studio recovery/regen download loops serial) | `agent/lib/upload-outputs.ts:72`; `studio-completeness.ts:266`; whole `agent/` subtree (0 `Promise.all`/`allSettled`) | small |
| 6 | low | Duplicated Resend POST boilerplate ×4 despite an existing `postOperatorAlert()` helper (3 used it, 4 hand-roll it) | `agent/lib/notify.ts:52,210,285,412` | small |
| 7 | low | **Dead export:** `resolveOrgForSlug()` zero callers (header self-describes as a stopgap awaiting an SSR refactor that already landed) | `frontend/lib/storage.ts:124` | trivial |
| 8 | low | Phase-progression/progress-% contract triplicated & already drifted (names, %, which phases exist) | `agent/lib/state-evaluation.ts:8` vs `executor.ts:813` vs `frontend/app/new/[id]/page.tsx:46` | small |
| 9 | low | Two oversized React page-components: HomePage (~453 LOC, ON the demo path but functional) + ProgressPage (~480 LOC) | `frontend/app/page.tsx:99`; `frontend/app/new/[id]/page.tsx:128` | medium |
| 10 | low | `find-state-file` selector pair drives the gallery's "current run" pick (was the S87 shadowing-bug fix) but only the agent copy is tested | `agent/lib/find-state-file.ts` vs `frontend/lib/find-state-file.ts` | small |
| 11 | low | Root `package.json` has no committed lockfile + a vestigial unpinned `@perplexity-ai/mcp-server` dep; `dependency-exceptions.md` register is stale (lists 0 vs 3 live deferred advisories) | `package.json:13`; `Documentation/dependency-exceptions.md` | small |
| 12 | low | **Plan-synthesizer retry re-sends an identical prompt** — missed regenerative retry-with-critique (the validation error is already in hand; never fed back to the model) | `agent/lib/plan-synthesizer.ts:392,397-463` | trivial |
| 13 | low | Stale skill cross-refs (mrpf-classify cites 3 absent memory files; codex-fallback + MEMORY.md dangling-wikilink 2 dead names) + `session-init` bloated to 650 LOC (~430 = an unbounded per-session journal burning context every load) | `~/.claude/skills/{mrpf-classify,codex-fallback,session-init}/SKILL.md` | small |

## Regenerative-AI + parallel-agent lens (your mission)
- **On-mission positives:** the plan-review loop is a real `synthesize → Gemini → integrate → Codex` self-correction loop; structured-output discipline is strong (Zod/JSON-schema, not raw parsing); no runtime self-modifying code means the sandbox guardrail is safe-by-absence. The `/research-compare` skill correctly overlaps the 3 research legs + launches studio products concurrently — so the **demo-relevant** generation parallelism already exists at the skill layer.
- **Clearest gap (#5):** the **worker daemon has zero concurrency primitives.** Highest-value, lowest-risk win is parallelizing the ~14 independent deliverable uploads (bounded `allSettled`, cap ~4, preserving the 0-byte refusal + `failed[]` accumulation). Adopt one small bounded-concurrency helper as the default worker pattern.
- **Regenerative quick win (#12):** convert the plan-synthesizer's blind retry into a true retry-with-critique (append the prior validation errors to the reprompt) — near-zero cost, a clean self-correction primitive directly on-mission.

## Completeness critic — what the audit did NOT cover, and how S183 already closed the top gaps
The audit was static/frozen. Its #1 flagged residual risk was **DATA/ENV, not code** — and **S183 (earlier this session) already closed the top three:**
1. **"No live demo-path smoke test" → DONE.** S183 logged in as Aira (admin-minted OTP), walked every route, and confirmed all **14 deliverables load (HTTP 200, correct content-types)**. ✅
2. **"Cross-read not proven by an actual two-tenant attempt" → DONE.** S183 ran the negative test: aira session → system-default slug = **404** on state/file/files. ✅
3. **"Didn't re-run `pnpm test`" → DONE at session start:** 663 agent / 142 frontend, 0 fail. ✅

Still genuinely uncovered (low-risk, noted for honesty): live-DB RLS re-verification (last confirmed S97), frontend perf/a11y/mobile-viewport of the gallery (out of scope — if screen-shared on an unusual viewport, a layout issue wouldn't have been caught), and the `.maybeSingle()` multi-org 500 smell (a FUTURE partner-cross-view concern, not single-tenant Aira risk).

## Prioritized post-demo remediation plan

**Right after the demo (highest value, low risk):**
1. Add the **untrusted-input parity guard** (#1) — SECURITY-labeled; same dogfooded pattern as the S178 storage-paths/attachment-constants guards. *Highest-value missing guard.*
2. Delete the **dead `resolveOrgForSlug()`** (#7) — trivial pure removal.
3. Convert **plan-synthesizer blind retry → retry-with-critique** (#12) — trivial, on-mission.

**Next (real debt, medium effort):**
4. **Cross-tier parity guards / shared types** for the ResearchJob/PipelineState mirror (#3) + the job-update allowlist single-source (#2) + find-state-file parity (#10) — the recurring "mirror defended only by a comment" theme.
5. **Companion tests** for `plan-review-gate.ts` and the other extraction/orchestration modules (#4).
6. **Worker bounded-concurrency helper** (#5) — parallelize upload/download tails (your parallelism goal).

**Later (hygiene):**
7. notify.ts dedup (#6), phase-table single-source (#8), React page-component splits (#9), lockfile/dep-register cleanup (#11), skill cross-ref + session-init journal trim (#13).

**Don't:** touch any of this before the demo. The repo is frozen; an unreviewed change tonight (no §11 gate possible at this hour) is strictly worse than shipping the verified-green state.
